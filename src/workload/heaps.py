"""Heaps' law vocabulary-growth exponent. Stub — Milestone 2.

beta = log(V) / log(N), where V = unique tokens, N = total tokens.
"""

from collections.abc import Sequence


def heaps_beta(tokens: Sequence[int]) -> float:
    raise NotImplementedError("Milestone M2 — see plan")
