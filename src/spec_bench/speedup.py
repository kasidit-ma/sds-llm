"""Aggregate per-sample speedups into per-dataset/per-config summaries. Stub — Milestone 3."""

from typing import Any


def aggregate_speedup(rows: list[dict[str, Any]]) -> list[dict[str, Any]]:
    raise NotImplementedError("Milestone M3 — see plan")
